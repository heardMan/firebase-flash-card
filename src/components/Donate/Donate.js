import React, { useEffect, useState } from 'react';
import { useAuth } from '../../contexts/Auth.js'
import { useNavigate } from 'react-router-dom';

import './Donate.css';

const Donate = () => {
    
    return (
        <div id='contact'>Donate Information</div>
    );
};

export default Donate;